// SupabaseSDKAuthClient was removed. supabase-swift cannot be added to this
// workspace because its "Auth" and "Storage" target names conflict with our
// local Auth and Storage SPM packages, causing package resolution to fail.
// The existing SessionManager handles all auth with direct GoTrue REST calls.
